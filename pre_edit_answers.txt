Before editing the code, record your answers to the following:

1. Where you think the new functionality belongs;

In the existing loop that processes each EVA, after the duration has been
converted into hours. The result can then be displayed with the other
printed results.

2. Which existing data or processing you can reuse;

I can reuse eva_data, the existing duration conversion, and the country and
date values already read from each record.

3. Which parts of the current program you expect to change;

I would add a variable to store the longest EVA found so far and add output
showing its date, country and duration.

4. Which parts you expect to leave untouched;

The JSON loading, country-total calculation, cumulative-hours calculation,
sorting, plotting and file-saving code should remain unchanged.

5. What existing behaviour might accidentally be affected.

Records with missing durations could cause errors if not skipped. Changing the
processing loop carelessly could also affect the country total or cumulative
plot, so the existing duration and date checks need to stay reliable.

Post-fix review

Inspect the difference between the original software and your version:

1. Did I change more code than I needed to?

No. The changes are limited to country totals, longest-EVA tracking and the
output for those results. The existing plotting code was left in place.

2. Would another developer understand what I changed?

Yes. The names country_totals and longest_eva describe the new behaviour, and
the output labels explain the values being displayed.

3. Did I duplicate behaviour that already existed elsewhere?

No. The existing duration conversion is reused for both features. The new code
only stores the totals and longest result while the records are being read.

4. Are there assumptions in my solution that another developer would need to discover?

The user types a country name when the program starts, and matching is not
case-sensitive. Country totals include records with a duration even when the
date is missing, while the cumulative plot still skips records without dates.

5. How confident am I that I have not broken existing behaviour?

I am reasonably confident. I tested USA, Russia and an unknown country. The
country totals and longest EVA were correct, and the cumulative plot was still
created.

6. Make any necessary corrections.

No further corrections were needed after testing. The generated PNG was left
out of the Git commits because it is a program output rather than source code.
